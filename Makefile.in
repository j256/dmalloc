#
# Copyright 1993 by the Antaire Corporation
#
# $Id: Makefile.in,v 1.27 1993/08/24 04:37:10 gray Exp $
#

HFLS	= malloc_dbg.h
OBJS	= arg_check.o chunk.o compat.o error.o heap.o malloc.o malloc_lp.o
OBJS_IN	= malloc_lp.o 
FILLFLS	= arg_check.c chunk.c error.c heap.c malloc_dbg.c malloc_lp.c \
	malloc_t.c sample.c

DEFS	= $(DEFINES)
INCS	= -I. -Iargv $(INCLUDES)
LIBS	= -L. -Largv -largv -lmalloc_dbg $(LIBRARIES)

CFLAGS	= -g $(CCFLS) -W -Wreturn-type -Wunused -Wpointer-arith -Wshadow \
	-Wswitch -Wcomment -Wtrigraphs -Wformat -Wchar-subscripts \
	-Wuninitialized -Wparentheses -Waggregate-return
LDFLAGS	= $(LLDFLAGS)
DESTDIR	= $(ROOT)
TESTS	= malloc_t
LIBRARY	= libmalloc_dbg.a
UTIL	= malloc_dbg

MANFILE	= malloc.info

PORTFLS	=  conf.h.in Makefile.all.in configure configure.in \
	ChangeLog Manifest NOTES README TODO mallocrc \
	arg_check.[ch] chunk.[ch] chunk_loc.h compat.[ch] dbg_tokens.h \
	dbg_values.h error.[ch] error_str.h error_val.h heap.[ch] malloc.c \
	malloc_dbg.[ch] malloc_lp.[ch] malloc_loc.h \
	version.h malloc_t.c sample.c \
	malloc.info malloc.texi malloc.ps texinfo.tex
PORTCFG	= contrib/README contrib/dec_notes

PARTS	= 1 2 3 4 5
MAXPART	= 5
SHAR1	= ChangeLog Makefile.in Manifest NOTES README TODO malloc.c
SHAR2	= chunk.c
SHAR3	= configure error.c error.h error_str.h error_val.h malloc.info
SHAR4	= chunk.h chunk_loc.h compat.c compat.h conf.h.in malloc.texi \
	malloc_t.c mallocrc
SHAR5	= configure.in dbg_tokens.h dbg_values.h heap.c heap.h malloc_dbg.c \
	malloc_dbg.h malloc_loc.h malloc_lp.c malloc_lp.h arg_check.c \
	arg_check.h sample.c version.h

.PHONY : all
all : $(LIBRARY) $(UTIL)

.PHONY : clobber
clobber :
	rm -f $(LIBRARY) $(TESTS) $(UTIL) part.? malloc.head malloc.info

.PHONY : distclean
distclean : clobber
	rm -f configure config.status Makefile conf.h
	rm -rf port

.PHONY : installincs
installincs : $(HFLS)
	install -cev -m 444 $(HFLS) $(DESTDIR)/inc

.PHONY : installlibs
installlibs : installincs $(LIBRARY) $(OBJS_IN) $(MANFILE)
	install -cev -m 644 $(LIBRARY) $(DESTDIR)/lib
	install -cev -m 444 $(OBJS_IN) $(DESTDIR)/lib
	install -cev -m 444 $(MANFILE) $(DESTDIR)/info

.PHONY : install
install : installlibs $(UTIL)
	install -cev $(UTIL) $(DESTDIR)/bin

# put situation specific lines here
$(LIBRARY) : $(OBJS)
	ar cr $(LIBRARY) $?
	ranlib $@
	- chmod 660 $@

$(UTIL) : malloc_dbg.o $(LIBRARY) $(ROOT)/lib
	rm -f $@
	$(CC) $(LDFLAGS) $@.o $(LIBS)
	mv a.out $@

.PHONY : port
# NOTE: no $(PORTFLS) here else we remake configure with autoconf
port : malloc.info
	@ answer 'Is it okay to remove the $@ directory? '
	rm -rf $@
	mkdir $@ $@/contrib
	cp $(PORTFLS) $@
	cp $(PORTCFG) $@/contrib
	mv $@/Makefile.all.in $@/Makefile.in
	@ echo ''
	@ echo 'Please rename $@ to malloc-version and tar up file'

.PHONY : patch
patch :
	@ echo 'Run fixpatch.pl on the patch file'

.PHONY : localfill
localfill : $(FILLFLS)
	fillproto $(FILLFLS)

tests : $(TESTS)

malloc_t : malloc_t.o $(LIBRARY)

malloc.head : malloc.info
	rm -f $@ $@.t
	sed -e '/^Malloc Debug Library/,/^   Gray Watson/p' -e 'd' < \
		malloc.info > $@.t
	mv $@.t $@

.PHONY : shar
shar : malloc.info malloc.head
	rm -f part.? part.tmp
	cd port ; $(foreach part, $(PARTS), \
		shar -b -n${part} -e${MAXPART} -o../part.${part} -t \
			"Do a 'sh ./configure' to configure the library" \
			${SHAR${part}} ; \
		)
	cat malloc.head > part.tmp
	echo '' >> part.tmp
	echo ---------------------------------------------------------------- \
		>> part.tmp
	cat part.1 >> part.tmp
	mv part.tmp part.1

include /usr/antaire/Default.make
include Makefile.dep
