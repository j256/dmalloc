- malloc.man is a text file having the user-documentation for the malloc
	subsystem
- Files contains a list of each of the file and what it does
- typing 'make' with GNU-make should be enough to build libmalloc.a
- typing 'make test' should build the malloc_t test program
- output from programs using libmalloc should be 'logfile' (see
	Antaire/logger.[ch] for details)
