-------------------------------------------------------------------------------
$Id: README.1,v 1.11 1992/12/17 23:30:23 gray Exp $
-------------------------------------------------------------------------------

INSTALLATION:

1) Run 'configure' to generate the Makefile and conf.h.  Examine conf.h
	(especially) as well as Makefile to make sure configure set things
	up correctly.
2) Typing 'make' should be enough to build libmalloc.a and malloc_debug program
3) Typing 'make test' should build the malloc_t program

-------------------------------------------------------------------------------

DOCUMENTATION:

Examine the malloc.info file.  Is is a gnu-info file having the
user-documentation for the malloc subsystem.  It is also suitable for reading
as text.

-------------------------------------------------------------------------------

NOTES:

- the system is definitely dependent on a good ansi-c compiler like gcc.
- the file Manifest contains a list of each of the file and what it does
- if you have any problems with porting issues feel free to send me mail.

thanks,
gray.watson@antaire.com

-------------------------------------------------------------------------------
